# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: Pack

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    
    strategy:
      matrix:
        include:
          - rid: win-x64
            os: windows-latest
          - rid: osx-x64
            os: macOS-12
          - rid: osx-arm64
            os: macOS-12

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x
      
      - name: Build Packages
        shell: bash
        run: |
          rm -r CeresGpuPrebuiltExternalTools/runtimes
          dotnet pack -c Release 

      - name: Upload Binary as Artifact
        uses: actions/upload-artifact@v3.1.1
        with:
          name: 'runtimes-${{ matrix.rid }}'
          # A file, directory or wildcard pattern that describes what to upload
          path: '${{ github.workspace }}/CeresGpuPrebuiltExternalTools/runtimes'
          if-no-files-found: error

  pack:
    name: Create NuGet Package
    runs-on: [ubuntu-latest]
    needs: build
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: false

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x
      
      - name: Pull Artifacts
        uses: actions/download-artifact@v3.0.1
        with:
          # Destination path
          path: '${{ github.workspace }}/artifacts'
      
      - name: Package
        shell: bash
        run: |
          cd "${GITHUB_WORKSPACE}"
          ls -R artifacts
          cp -r "artifacts/lib-win-x64/runtimes/" "runtimes/"
          cp -r "artifacts/lib-osx-x64/runtimes/" "runtimes/"
          cp -r "artifacts/lib-osx-arm64/runtimes/" "runtimes/"
          ls -R runtimes
          dotnet pack -c Release

      - name: Upload Package as Artifact
        uses: actions/upload-artifact@v3.1.1
        with:
          # Artifact name
          name: 'nuget_package' # optional, default is artifact
          # A file, directory or wildcard pattern that describes what to upload
          path: 'CeresGpuPrebuiltExternalTools/bin/Release/CeresGpuPrebuiltExternalTools.*.nupkg'
          if-no-files-found: error
